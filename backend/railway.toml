[build]
builder = "railpack"

[deploy]
startCommand = "php bootstrap-db.php && php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=$PORT"
preDeployCommand = "php bootstrap-db.php && php artisan migrate --force && php artisan db:seed --force"